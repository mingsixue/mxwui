<view class="pageLogin" wx:if="{{isLoading}}"><nav-bar title="授权登录" barBg="#2fc178" /><view class="heads" style="padding-top:{{navHeight}}px;"><view class="avatars" bind:tap="handleAvatarUpload"><view wx:if="{{!avatar}}" class="default"></view><image wx:else class="avatar" mode="aspectFill" src="{{avatar}}" /><view wx:if="{{!hasRegister}}" class="upload"></view></view></view><view class="infos"><view class="txt">您未授权文聚禾小程序获取你的信息，将无法正常使用小程序的功能。如需要正常使用，请先进行注册</view><view wx:if="{{!hasRegister}}" class="rows"><view class="label">昵称：</view><input class="input" type="nickname" placeholder="请输入昵称" bind:input="inputNickName" /></view><view wx:if="{{!hasRegister}}" class="rows"><view class="label">手机号码：</view><input class="input" type="number" placeholder="请输入手机号码" maxlength="11" bind:input="inputPhone" /></view><view wx:if="{{!hasRegister}}" class="btnLogin" bind:tap="handleSave">注册登录</view><view wx:if="{{hasRegister}}" class="btnLogin" bind:tap="handleLogin">登录</view><view class="btnCancel" bind:tap="handelCancel">取消</view></view><copyright isFixed="{{true}}" height="140" /></view>